apiVersion: apps/v1
kind: Deployment
metadata:
  name: mmitm-mobile
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mmitm-mobile
  template:
    metadata:
      labels:
        app: mmitm-mobile
    spec:
      containers:
        - name: mmitm-mobile
          image: your-registry/mmitm-mobile:latest  # Will be overridden by Kustomize
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              name: http
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5